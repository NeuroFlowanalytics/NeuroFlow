
---

#### 2. **src/main.py** (Script principal)
```python
# Import des libs Azure
from azure.identity import DefaultAzureCredential
from azure.mgmt.costmanagement import CostManagementClient

def analyze_azure_costs():
    """Identifie les économies potentielles sur Azure"""
    credential = DefaultAzureCredential()
    client = CostManagementClient(credential)
    
    # Simulation de données (à remplacer par l'API réelle)
    unused_vms = {"VM-Prod": 15, "VM-Dev": 42}  # % d'inutilisation
    
    print(f"Potential savings: ${sum(unused_vms.values()) * 100}/month")
    return unused_vms

if __name__ == "__main__":
    analyze_azure_costs()